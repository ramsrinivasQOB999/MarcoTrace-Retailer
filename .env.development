# Dev: same-origin /api → Vite proxies to Spring (see vite.config.ts). Set to false to call Spring directly (CORS must allow your UI origin).
VITE_API_PROXY=true

# Direct API URL when not using proxy (e.g. production build) or for documentation
VITE_API_BASE_URL=http://localhost:8090
